# Reconciliation response recovery — September 6, 2026

The reported reconciliation action immediately showed `Unexpected token '<'` while its receipt remained paid. The user subsequently supplied repeated HTTP 502 console errors, then the Cloudflare-branded HTML response reporting a host error for `surge-sand.basalthq.com`. Incident time: September 6, 2026, 23:30:16 UTC. Cloudflare Ray ID: `a3714291eebe5344`. This establishes the response source but does not establish that the payment or sweep failed. The immediate timing does not support attributing this incident to a long-running sweep timeout. Origin logs are needed to identify the underlying failure.

The browser now reports invalid responses with their HTTP status and preserves an unknown outcome. It generates a run ID before submission, saves it in session storage, and can check the recorded run through GET requests after a lost response or refresh. It never automatically repeats the mutation. A scan is needed after refresh to display the corresponding session row.

The endpoint records an audit run, reserves one active run per receipt, and returns HTTP 202 before executing the existing targeted reconciler through Next.js `after`. Repeat submissions reuse the active run. Receipt payment acceptance is recorded separately from settlement, including the canonical paid Stripe session ID. Invalid worker responses remain unknown and block another audit dispatch. The existing transfer claims, split routing, and fee calculations are reused.

`after` is tied to the server process, not an external durable job queue. A process interruption can leave a queued or running journal requiring operational inspection; no time-based expiry grants permission to sweep again. This change does not establish successful production settlement or resolve an unidentified proxy/deployment rejection.

Validation: 84 focused tests passed across audit response parsing, audit execution, session binding, background polling, and targeted reconciliation. The analytics dependency graph passed TypeScript checking. Local Next.js GET and POST probes without authentication both returned HTTP 401 with JSON; no payment or sweep was initiated. Production deployment and diagnosis of the original HTML response remain outstanding.

Follow-up: application failures now include a request ID and operation stage in the JSON response and server log. The client displays these diagnostics. Run reservation handles MongoDB duplicate-key code 11000 as well as Cosmos conflicts, so concurrent submissions reuse the active run. This fixes a discovered concurrency issue in the new reservation code, not a proven cause of the originally reported HTML. All 33 audit endpoint/client tests and the analytics typecheck passed after these changes.

Read-only remote checks against the affected hostname returned JSON HTTP 401 for GET at 23:32:32 UTC and an unauthenticated empty POST at 23:47:06 UTC. Response headers identify Phusion Passenger 6.1.8. These probes establish current reachability through authentication, not authenticated reconciliation success. The supplied reconciliation payload has no `runId`, indicating the older client was used. No real reconciliation was submitted.

Next diagnostic: inspect origin reverse-proxy and Passenger/Node logs around 23:30:16 UTC, matching the route and Ray ID where available. Check for upstream connection resets, application exits, worker restarts, and the application's own error output. No origin administration/log connection was available in this session. Cloudflare's [502/504 documentation](https://developers.cloudflare.com/support/troubleshooting/http-status-codes/cloudflare-5xx-errors/error-502-504/) distinguishes this branded origin-error response from an unbranded Cloudflare-originated error. Do not infer a specific process crash or infrastructure fix from the HTML alone.
